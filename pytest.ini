[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
env_files = .env.test
addopts =
    --tb=short
    --cov=backend/app
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    -v
